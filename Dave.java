// import 
import java.util.Arrays;

class Dave{
    public static void main(String[] args){
        Maze derp = new Maze(4,4);
        // derp.move('W');
        // System.out.println(Arrays.toString(derp.getPos()));
        // boolean gudMove = derp.move('N');
        // System.out.println(gudMove?"Okay move!":"Bad move!");
        // System.out.println(Arrays.toString(derp.getPos()));
        // gudMove = derp.move('E');
        // System.out.println(gudMove?"Okay move!":"Bad move!");
        // System.out.println(Arrays.toString(derp.getPos()));
        // derp.move('S');

        derp.testStuff();
    }
}